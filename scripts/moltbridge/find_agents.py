import requests
import json
import time
import os

GITHUB_TOKEN = os.environ.get("GITHUB_TOKEN") # Will need to get this or use gh api

def search_github(query):
    # Let's just use the gh cli to make it easier to authenticate
    pass

